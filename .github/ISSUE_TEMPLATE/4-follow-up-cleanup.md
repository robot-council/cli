---
name: Follow-up cleanup
about: Residual work split out of a shipped change or a review.
type: Task
labels: development
---

<!--
  Skeleton only — .claude/skills/writing-issues/SKILL.md is the source of truth.
  Delete the sections you don't need, and delete these comments before submitting.

  Title: imperative, naming the residue precisely. No trailing period, no `chore:` prefix.
  This archetype is deliberately short — make deferred work visible with a home, don't
  re-argue the parent change.
  Never add a "generated by AI during triage" line.
  After filing: add exactly one of `afk` / `hitl` (most cleanups are `afk` — the parent
  pinned the design), and assign it. If the parent must land first, add a native
  `blocked_by` dependency edge rather than a "Blocked by" line in the body, which drifts.
-->

Follow-up to #

<!-- 1-3 sentences: what was deferred, and why it wasn't done in the parent change. If a
     review surfaced it, say which — `Surfaced by the #N adversarial review.` -->

## Scope

<!-- A bounded list of what this covers. Bounded is the point. -->

## Acceptance criteria

- [ ]
- [ ] A test covers the change.

## Out of scope

## References
