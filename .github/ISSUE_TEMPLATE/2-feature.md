---
name: Feature
about: Build something new. The title names the deliverable as an imperative outcome.
type: Feature
labels: development
---

<!--
  Skeleton only — .claude/skills/writing-issues/SKILL.md is the source of truth.
  Delete the sections you don't need, and delete these comments before submitting.

  Title: imperative, verb-first, naming what this ACCOMPLISHES — not a topic label.
  No trailing period, no `feat:` prefix. Backtick class names, config keys, and commands.
  Never add a "generated by AI during triage" line.
  Before filing, search the tracker for the same concept — including closed issues.
  After filing: add exactly one of `afk` / `hitl`, and assign it. If this carries an
  unresolved design fork it is `hitl` (with `decision-fork`) — add `## Decisions to resolve`
  and settle it per .claude/rules/design-decision-forks.md before building.
-->

<!-- Lede: 1-3 sentences framing the problem and the desired outcome, often opening with
     provenance (`Spun off from #N.`). -->

## Why

<!-- On a feature this carries the weight: the forces that make this necessary now. -->

## Current state

## What we're building

## Acceptance criteria

<!-- Observable outcomes, not implementation steps — name the command, config key, class, or
     output. Bound the unaffected cases too. -->

- [ ]
- [ ] Tests cover the behavior above.

## Out of scope

## References
