#!/usr/bin/env php
<?php

// The whole command line is in `entry.php`, so the launcher can require it without this line's
// shebang reaching stdout (robot-council/cli#280).
require __DIR__.'/entry.php';
